# Run 'make' in 'yosys'

# Run 'make' in 'yosys/manual/verification/addmodule', which will generate a new pass 'addmodule.so' in main folder

# Run 'make' in 'yosys/manual/verification/show_regs_mems', which will generate a new pass 'show_regs_mems.so' in main folder

# Run 'make' in 'yosys/manual/verification/stuttering', which will generate a new pass 'stuttering.so' in main folder